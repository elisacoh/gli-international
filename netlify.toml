[build]
  base = "frontend"
  command = "npm run build"
  publish = ".next"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  included_files = ["netlify/functions/**"]

#[[plugins]]
#  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "18"

# Redirects for internationalization
# Disabled: Middleware now handles automatic locale detection based on browser language
# [[redirects]]
#   from = "/"
#   to = "/fr"
#   status = 302
#   force = false

#[[redirects]]
#  from = "/*"
#  to = "/:splat"
#  status = 200
#

